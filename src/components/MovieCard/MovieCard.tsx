import Header from "../Header/Header";

const MovieCard: React.FC = () => {
  return (
    <div>
      <Header />
      <h2>Детали фильма</h2>
      {/* Здесь будут детали выбранного фильма */}
    </div>
  );
};

export default MovieCard;